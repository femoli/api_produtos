fastapi==0.95.2        # Framework web para construir APIs
uvicorn==0.22.0        # Servidor ASGI para rodar a aplicação FastAPI
httpx==0.24.0          # Cliente HTTP assíncrono para chamadas externas (opcional)
pytest==7.2.2          # Framework para testes
httpx==0.24.0          # Cliente HTTP para chamadas a APIs externas (opcional)
pytest-asyncio==0.20.1 # Suporte para testes assíncronos com pytest
mypy==0.991            # Verificador de tipo para Python (opcional, mas recomendado)
flake8==4.0.1          # Ferramenta de linting para verificar a conformidade com o estilo de código
pylint==2.17.3         # Ferramenta de linting e análise estática de código (opcional)
